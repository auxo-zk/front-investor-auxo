version: '3.8'

services:
    frontend-investor:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - '3113:3000'
        environment:
            NODE_ENV: production # Set your desired Node environment

